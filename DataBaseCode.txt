CREATE TABLE Users (
	id int(10) UNSIGNED AUTO_INCREMENT,
	name VARCHAR(45),
	surname VARCHAR(45), 
	login VARCHAR(45),
	email VARCHAR(45),
	password VARCHAR(45),
	avatar LONGTEXT,

	PRIMARY KEY (User_id)
);
